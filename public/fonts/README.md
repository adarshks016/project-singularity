Drop a display font here as `abolda.woff2` (or `.woff` / `.otf`) and it is
picked up automatically by the `@font-face` rule in `src/styles/base.css`.

If no file is present the stack falls through to Melodrama, then Gabarito.
Nothing breaks either way.
